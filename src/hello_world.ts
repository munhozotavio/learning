const pName = "Jonas!";
console.log('Hello, ' + pName + '!');
// --------------------------------- //

class Product{
  pName: string;
  pValue: number;

  constructor(pName: string, pValue: number){
    this.pName = pName;
    this.pValue = pValue;
  }
}
